spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id:  315260072043-jgd7j5t5o2ooafn2ogdhtedqfkf9rgvo.apps.googleusercontent.com
            client-secret: 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
            scope: openid, email, profile
        provider:
          google:
            issuer-uri: https://accounts.google.com

  servlet:
    multipart:          # <-- stessa indentazione di "security", quindi nessun conflitto
      max-file-size: 10MB
      max-request-size: 10MB

  datasource:
    url: jdbc:mysql://localhost:3306/veely?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Europe/Rome
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

jpa:
    hibernate:
      ddl-auto: update
      # Forza l’uso del dialect MySQL 8.x (o 5.x a seconda della tua versione)
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true

    servlet:
      multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080
  
thymeleaf:
    cache: false
  
# ----------------- fuori da "spring" -----------------
veely:
  storage:
    root: uploads
